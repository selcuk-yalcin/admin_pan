/** Post-login default and logo home (admin dashboard). */
export const APP_HOME_PATH = "/dashboard";

/**
 * Mevzuat Botu (legislation chatbot). Component and route stay in the repo;
 * when false, /legislation-chatbot redirects to APP_HOME_PATH.
 */
export const LEGISLATION_CHATBOT_ENABLED = false;
