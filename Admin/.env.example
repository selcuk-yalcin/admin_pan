# HSG245 Smart Report - Environment Variables
# ⚠️ IMPORTANT: Copy this file to .env.local (NOT tracked in git)

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================

# ⚠️ UPDATE THIS: Your FastAPI backend URL
# Development (local):
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000

# Production options (choose one):
# Option 1: Railway.app (recommended)
# NEXT_PUBLIC_BACKEND_API_URL=https://your-app-name.up.railway.app

# Option 2: Render.com
# NEXT_PUBLIC_BACKEND_API_URL=https://your-app-name.onrender.com

# Option 3: Vercel Serverless (if you deploy backend to Vercel too)
# NEXT_PUBLIC_BACKEND_API_URL=https://your-backend.vercel.app

# =============================================================================
# DEPLOYMENT INSTRUCTIONS
# =============================================================================
# 
# LOCAL DEVELOPMENT:
# 1. Copy this file: cp .env.example .env.local
# 2. Keep: NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000
# 3. Start backend: cd /Users/selcuk/Desktop/HSE_AgenticAI && python -m uvicorn api.main:app --reload --port 8000
# 4. Start frontend: npm run dev
#
# PRODUCTION DEPLOYMENT:
# 1. Deploy backend to Railway/Render (see deployment guide)
# 2. Get your backend URL (e.g., https://hse-api.up.railway.app)
# 3. Add to Vercel environment variables:
#    - Go to Vercel Dashboard → Your Project → Settings → Environment Variables
#    - Add: NEXT_PUBLIC_BACKEND_API_URL = https://your-backend-url
#    - Apply to: Production, Preview, Development
# 4. Redeploy frontend
#
# =============================================================================

# Other environment variables (if needed)
# NEXT_PUBLIC_API_TIMEOUT=60000
# NEXT_PUBLIC_ENABLE_DEBUG=false
